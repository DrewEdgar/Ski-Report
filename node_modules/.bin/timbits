#!/bin/sh
if [ -x "`dirname "$0"`/coffee" ]; then
  "`dirname "$0"`/coffee"  "`dirname "$0"`/../timbits/bin/timbits" "$@"
  ret=$?
else 
  coffee  "`dirname "$0"`/../timbits/bin/timbits" "$@"
  ret=$?
fi
exit $ret
