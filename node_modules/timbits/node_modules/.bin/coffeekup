#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../coffeekup/bin/coffeekup" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../coffeekup/bin/coffeekup" "$@"
  ret=$?
fi
exit $ret
